/* tslint:disable:no-unused-variable */

import { NumberCorrectPipe } from './number-correct.pipe';

describe('NumberCorrectPipe', () => {
  it('create an instance', () => {
    let pipe = new NumberCorrectPipe();
    expect(pipe).toBeTruthy();
  });
});
